<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Order Success</title>
    <link rel="stylesheet" href="css/bootstrap.min.css">
    <style>
        .success-container {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 100vh;
            text-align: center;
        }

        .success-icon {
            font-size: 5rem;
            color: green;
            margin-bottom: 20px;
        }

        .success-message {
            font-size: 1.5rem;
            margin-bottom: 20px;
        }

        .btn-container {
            display: flex;
            gap: 20px;
            justify-content: center;
        }

        .btn-container a {
            text-decoration: none;
        }

        .order-details {
            text-align: left;
            margin-bottom: 20px;
        }

        .order-details h4 {
            margin: 10px 0;
        }
    </style>
</head>

<body>
    <!-- Order Success Container -->
    <div class="success-container">
        <div class="success-icon">✔️</div>
        <h1 class="success-message">Your Order Has Been Successfully Placed!</h1>

        <!-- Order Details Section -->
        <div class="card text-success mb-3" style="max-width: 30rem;">
            <div class="card-header text-center">
                <h3>Order Details</h3>
            </div>
            <div class="card-body">
                <h5 class="card-text">Total Items: <%= order.totalQuantity %></h5>
                <h5 class="card-text">Coupon Applied: <%= order.couponDiscount %></h5>
                <h5 class="card-text">Total Discount: ₹<%= order.totalDiscount %></h5>
                <h5 class="card-text">Total Amount: ₹<%= order.totalAmount %></h5>
            </div>
        </div>
        

        <!-- Buttons Section -->
        <div class="btn-container">
            <!-- Button to go back to the Home Page -->
            <a href="/" class="btn btn-primary">Go Back to Home</a>
            <!-- Button to view Order Details -->
            <a href="/order" class="btn btn-secondary">View Orders</a>
        </div>
    </div>

    <!-- Include Bootstrap JS -->
    <script src="js/bootstrap.min.js"></script>
</body>

</html>
